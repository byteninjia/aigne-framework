import { create } from "@arcblock/ux/lib/Theme";

const theme = create({
  palette: {
    primary: {
      main: "#108cc6",
      contrastText: "#fff",
    },
  },
  typography: {
    fontFamily: '"Lexend", sans-serif',
  },
});

export default theme;
